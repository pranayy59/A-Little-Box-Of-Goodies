import {ImageResponse} from 'next/og';
import {db} from '@/lib/db';
export const dynamic='force-dynamic';
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const p=await db.package.findUnique({where:{id:(await params).id}});if(!p||p.status!=='sent')return new Response('Package not found',{status:404});return new ImageResponse(<div style={{background:'#F7F1E6',width:'100%',height:'100%',display:'flex',alignItems:'center',justifyContent:'center',fontFamily:'sans-serif',color:'#433d35'}}><div style={{display:'flex',flexDirection:'column',background:'#fffdf7',border:'2px solid #cbbb9f',borderRadius:10,padding:52,width:1000,boxShadow:'10px 12px 0 #e8dfce'}}><div style={{display:'flex',justifyContent:'space-between',fontSize:20,letterSpacing:3,borderBottom:'2px dashed #cbbb9f',paddingBottom:22}}><span>THE LITTLE GOODIES POST</span><span>HANDLE WITH LOVE</span></div><div style={{display:'flex',fontSize:54,marginTop:36}}>to: {p.to}</div><div style={{display:'flex',fontSize:32,marginTop:20,color:'#a46447'}}>from: {p.isAnonymous?'A secret admirer':p.from}</div><div style={{display:'flex',fontSize:22,marginTop:36,borderTop:'2px dashed #cbbb9f',paddingTop:24}}>A little box of goodies. A whole lot of love.</div></div></div>,{width:1200,height:630,headers:{'Cache-Control':'public, max-age=0, must-revalidate'}});}
